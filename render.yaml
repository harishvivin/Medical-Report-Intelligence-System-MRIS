services:
  - type: web
    name: medical-report-intelligence-system
    env: python
    region: oregon
    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r backend/requirements.txt
      cd frontend && npm install && npm run build && cd ..
    startCommand: PYTHONPATH=backend python -m uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.10.12
